<?xml version="1.0" encoding="utf-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>CMDFMIssueIndicator__c</fullName>
    <externalId>false</externalId>
    <formula>IF(AND(( Next_SLA_End__c - now() ) * 24 * 60&gt;0,( Next_SLA_End__c - now() ) * 24 * 60&lt;20),'👀',
IF(OR(
AND(ISPICKVAL(Status,'Pending'),
FLOOR( MOD( (now() -  LastModifiedDate) * 24, 24 ) ) &gt;16 )
),'🟡',IF(OR(
AND(OR(ISPICKVAL(Status,'New'),ISPICKVAL(Status,'Response Received')),SLA_Breached__c)
),'⏰',IF(ISPICKVAL(Priority,'VIP'),'🥇',''))))</formula>
    <inlineHelpText>Cases stay in Pending status for more than 16 Hours.</inlineHelpText>
    <label>Case Age Issue Indicator</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
